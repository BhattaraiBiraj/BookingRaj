<% layout("/layouts/boilerplate") %>
<body>
    <div class="row">
          <div class="col-8 offset-2">
                <h1>This is your details</h1> 
                <!-- <%= listing.title %><br>
                <%= listing.description %><br>
            ₹ <%= listing.price.toLocaleString("en-IN") %><br>
                <%= listing.location %><br><br> -->


            <div class="card">
            <img src="<%=listing.image%>" class="card-img-top show-img" alt="...">
            <div class="card-body">
                <p class="card-text">
                    <b><%= listing.title %></b><br><br>
                    <%= listing.description %><br><br>
                     <b>₹ <%= listing.price.toLocaleString("en-IN") %>/ Night</b><br><br>
                     <%= listing.location %>
            </div>
            </div>
            <div class="btns">
                        <form method="get" action="/listings/<%=listing.id%>/edit" >
                            <button class="btn edit-btn show-edit-btn">Edit</button>
                        </form>
                        <form action="/listings/<%= listing.id %>?_method=Delete" method="post">
                            <button class="btn delete-btn">Delete</button>
                        </form>
            </div>
     

    </div>
    </div>

   
</body>
